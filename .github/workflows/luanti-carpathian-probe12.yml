name: TEKNIK Luanti Carpathian Probe12 Benchmark

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'scripts/world/reference/luanti_carpathian_fast_prototype.gd'
      - 'scripts/world/reference/luanti_carpathian_probe12_prototype.gd'
      - 'tests/luanti_carpathian_probe12_gate.gd'
      - '.github/workflows/luanti-carpathian-probe12.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  benchmark:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install runtime dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends unzip wget libgl1 libx11-6 libxcursor1 libxinerama1 libxi6 libxrandr2 libasound2t64
      - name: Install Godot 4.3
        run: |
          wget -q https://github.com/godotengine/godot/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip -O /tmp/godot.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          mv /tmp/godot/Godot_v4.7.1-stable_linux.x86_64 /tmp/godot/godot
          chmod +x /tmp/godot/godot
      - name: Strict parse
        run: |
          mkdir -p artifacts/carpathian-probe12
          set -o pipefail
          XDG_DATA_HOME="$RUNNER_TEMP/carpathian-probe12-parse" timeout 120 \
            /tmp/godot/godot --headless --path . --editor --quit \
            2>&1 | tee artifacts/carpathian-probe12/parse.log
          if grep -E 'SCRIPT ERROR: Parse Error|Failed to load script' artifacts/carpathian-probe12/parse.log; then exit 1; fi
      - name: Characterize and benchmark probe12 adapter
        run: |
          set -o pipefail
          XDG_DATA_HOME="$RUNNER_TEMP/carpathian-probe12-run" timeout 900 \
            /tmp/godot/godot --headless --audio-driver Dummy --path . \
              --script res://tests/luanti_carpathian_probe12_gate.gd \
            2>&1 | tee artifacts/carpathian-probe12/probe12.log
          grep -Fq 'LUANTI_CARPATHIAN_PROBE12_COMPLETE' artifacts/carpathian-probe12/probe12.log
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: luanti-carpathian-probe12-evidence
          path: artifacts/carpathian-probe12/
          if-no-files-found: error
