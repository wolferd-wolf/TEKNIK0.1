name: TEKNIK Playable World Mining Port

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: playable-world-mining-port-${{ github.ref }}
  cancel-in-progress: true

jobs:
  playable-world-mining-port:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install runtime dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends unzip wget libgl1 libx11-6 libxcursor1 libxinerama1 libxi6 libxrandr2 libasound2t64

      - name: Install Godot 4.3
        run: |
          wget -q https://github.com/godotengine/godot/releases/download/4.3-stable/Godot_v4.3-stable_linux.x86_64.zip -O /tmp/godot.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          mv /tmp/godot/Godot_v4.3-stable_linux.x86_64 /tmp/godot/godot
          chmod +x /tmp/godot/godot
          /tmp/godot/godot --version

      - name: Parse project strictly
        run: |
          mkdir -p artifacts
          set -o pipefail
          timeout 120 /tmp/godot/godot --headless --path . --editor --quit 2>&1 | tee artifacts/playable-world-port-parse.log
          if grep -E 'SCRIPT ERROR: Parse Error|Failed to load script' artifacts/playable-world-port-parse.log; then
            exit 1
          fi

      - name: Validate imported world and mining pipeline
        run: |
          set -o pipefail
          timeout 300 /tmp/godot/godot \
            --headless \
            --audio-driver Dummy \
            --path . \
            --script res://tests/playable_world_mining_port_gate.gd \
            2>&1 | tee artifacts/playable-world-mining-port.log
          grep -F "PLAYABLE_WORLD_MINING_PORT_GATE_PASS" artifacts/playable-world-mining-port.log

      - name: Upload validation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playable-world-mining-port-evidence
          path: artifacts/
          if-no-files-found: error
